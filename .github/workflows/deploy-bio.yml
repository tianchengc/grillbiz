name: Deploy Bio Page to Cloudflare Pages

on:
  push:
    branches:
      - main
    paths:
      - 'grill-bio/boilerplate/src/data/site-config.ts'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-node: 20
          cache: 'npm'
          cache-dependency-path: 'grill-bio/boilerplate/package-lock.json'

      - name: Install Dependencies
        run: |
          cd grill-bio/boilerplate
          npm ci

      - name: Build Next.js Static Export
        run: |
          cd grill-bio/boilerplate
          npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: 'grillbiz-bio'
          directory: 'grill-bio/boilerplate/out'
          gitBranch: 'main'
